# Copie este arquivo para .env e preencha com os dados reais do seu banco.
# O arquivo .env NAO deve ser versionado (esta no .gitignore).
DATABASE_URL=postgres://usuario:senha@host:porta/banco?sslmode=disable
